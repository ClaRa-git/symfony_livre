<div class="game-card">
	<img src="{{asset('images/covers/' ~ book.imagePath)}}" alt="{{book.title}}" class="game-image">
	<div class="game-info">
		<h2 class="game-title">{{book.title|slice(0,40)}}</h2>
		<div class="game-info-container">
			<a href="{{path('app_detail_book', {'id': book.id})}}" class="btn btn-detail">Voir détail</a>
		</div>
	</div>
</div>
