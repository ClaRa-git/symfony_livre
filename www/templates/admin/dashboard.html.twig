{% extends "base.html.twig" %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <h1>Dashboard</h1>

    <div class="action-buttons">
        <a href="{{path('app_author_index')}}" class="btn btn-edit">Voir les auteurs</a>
        <a href="#" class="btn btn-edit">Voir les séries</a>
        <a href="{{path('app_book_index')}}" class="btn btn-edit">Voir les livres</a>
        <a href="{{path('app_type_index')}}" class="btn btn-edit">Voir les genres</a>
        <a href="{{path('app_user_index')}}" class="btn btn-edit">Voir les utilisateurs</a>
    </div>
{% endblock %}
