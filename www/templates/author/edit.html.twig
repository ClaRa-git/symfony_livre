{% extends 'base.html.twig' %}

{% block title %}Edition de l'auteur{% endblock %}

{% block body %}

    <div class="game-form-container">    
        <h1>Modifier l'auteur</h1>

        <h2>{{author.firstname}} {{author.name}}</h2>

        {{ include('author/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="action-buttons">
            <a href="{{path('app_author_index')}}" class="btn btn-back">
                <i class="fas fa-arrow-left"></i>
                Retour à la liste
            </a>
            {{ include('author/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
