{% extends 'base.html.twig' %}

{% block title %}Edition de la série{% endblock %}

{% block body %}
    <h1>Modifier la série</h1>

    <h2>{{ serie.title }}</h2>

    {{ include('serie/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="action-buttons">
        <a href="{{path('app_serie_index')}}" class="btn btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
        {{ include('serie/_delete_form.html.twig') }}
    </div>
{% endblock %}
