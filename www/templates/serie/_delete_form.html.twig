<form method="post" action="{{ path('app_serie_delete', {'id': serie.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ serie.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
