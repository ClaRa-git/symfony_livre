{% extends 'base.html.twig' %}

{% block title %}Edition de l'éditeur{% endblock %}

{% block body %}
    <h1>Modifier l'éditeur</h1>

    <h2>{{ editor.name }}</h2>

    {{ include('editor/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <div class="action-buttons">
        <a href="{{path('app_editor_index')}}" class="btn btn-back">
            <i class="fas fa-arrow-left"></i>
            Retour à la liste
        </a>
        {{ include('editor/_delete_form.html.twig') }}
    </div>
{% endblock %}
