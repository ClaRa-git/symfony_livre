<form method="post" action="{{ path('app_editor_delete', {'id': editor.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ editor.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
