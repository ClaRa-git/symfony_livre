{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <div class="card" style="width: 50rem;">
        <div class="card-body">
            <p class="card-text">{{type.label}}</p>
        </div>
    </div>

    <div class="action-buttons">
		<div>
			{# suppression #}
			{% include "type/_delete_form.html.twig" with {'button_class': 'btn btn-delete'} %}
		</div>
		<div>
			<a href="{{ path('app_type_edit', {'id': type.id}) }}" class="btn btn-edit">Editer</a>
		</div>
		<div>
			<a href="{{path('app_type_index')}}" class="btn btn-back">
				<i class="fas fa-arrow-left"></i>Retour à la liste</a>

		</div>
    </div>
{% endblock %}
